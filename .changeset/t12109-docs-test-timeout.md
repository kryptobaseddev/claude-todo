---
"@cleocode/cleo": patch
---

T12109: docs-canonical-surface integration test — raise the spawned-CLI timeout from 30s to 120s and retry once on a timeout kill (status null), so a loaded CI runner no longer fails the whole shard
